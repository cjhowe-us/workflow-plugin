{
  "name": "workflow",
  "version": "2.0.0",
  "description": "Workflow primitive for the artifact ecosystem. Ships the worker agent, python hooks, the `execution` scheme + `execution-gh-pr` storage, the `/workflow` entry skill, and four base workflows (default, conductor, plan-do, write-review). Depends on the artifact plugin for schemes, storage, templates, and the `/artifact` entry.",
  "author": {
    "name": "Christian Howe",
    "email": "cjhowe@users.noreply.github.com"
  },
  "license": "Apache-2.0",
  "requires": {
    "artifact": ">=2.0.0"
  },
  "keywords": [
    "workflow",
    "orchestrator",
    "worker",
    "dispatch",
    "agent-teams"
  ]
}
